/*
 * oApogee core: flight state name tables.
 *
 * STUB. This file is intentionally empty of definitions. It implements the
 * accessors declared in the generated header gen/oa_states.h, whose enumeration
 * comes from data/flight-phases.yaml. The tables it will hold are expanded from
 * OA_STATE_LIST so that they cannot disagree with the enumeration.
 *
 * Nothing here has run on hardware.
 */

#include "oa_states.h"

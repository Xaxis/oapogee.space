/*
 * oApogee core: config implementation.
 *
 * STUB. This file is intentionally empty of definitions. This commit is the
 * contract only: the headers, the field tables, the build system and the two
 * enforcement artifacts. The implementations land next, written against
 * oapogee/oa_config.h, and nothing in that header may be changed to suit an
 * implementation without changing the specs it was derived from first.
 *
 * The include below is not decoration. It is what makes this file fail to
 * compile if the header does not stand on its own, which is the one property a
 * header this many things depend on has to have.
 *
 * Nothing here has run on hardware.
 */

#include "oapogee/oa_config.h"
